Portable C functions from musl 1.2.6, using the archive and checksum recorded in ../musl-headers/SOURCE.json. Internal aliases are omitted; glob uses the public strchrnul declaration. These functions execute in BPF.
scandir copies Picolibc fixed-size dirents; inet_aton uses its public name.
